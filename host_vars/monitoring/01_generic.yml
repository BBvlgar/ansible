---

docker_host:     no
traefik_enabled: no
disallowed_users: []
sophos_av_disableMTD: yes
remote: yes
debug:  no
host_ufw_rules:
  # Wireguard
  - rule: allow
    port: "60002"
    proto: "tcp"
  # Wireguard
  - rule: allow
    port: "60002"
    proto: "udp"

...
